#ifndef RUNEHINCLUDED
#define RUNEHINCLUDED

extern unsigned char * runewr(unsigned char *const, unsigned);
extern const unsigned char * runerd(const unsigned char *, unsigned *const);

extern unsigned runerun(const unsigned char *const);
extern unsigned runelen(unsigned);

#endif
