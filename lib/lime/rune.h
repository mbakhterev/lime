#ifndef RUNEHINCLUDED
#define RUNEHINCLUDED

extern unsigned char * wrrune(unsigned char *const, unsigned);
extern const unsigned char * rdrune(const unsigned char *, unsigned *const);

extern unsigned runerun(const unsigned char *const);
extern unsigned runelen(unsigned);

#endif
